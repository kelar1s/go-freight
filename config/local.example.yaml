env: "local"

http_server:
  address: "localhost:8080"
  timeout: 4s
  idle_timeout: 60s

database:
  host: "localhost"
  port: 5433
  user: "postgres"
  password: "${DB_PASSWORD}"
  db_name: "gofreight"
  ssl_mode: "disable"